# DM-private working files — never published to the live site.
# Anything inside dm-private/ is invisible to git, so it cannot be
# committed to the public repo by accident.
dm-private/

# Local scratch
*.local.html
*.local.json

# build-tool and test leftovers
__pycache__/
*.pyc
node_modules/

# dev screenshots — keep them out of the public site
Claude outputs/
